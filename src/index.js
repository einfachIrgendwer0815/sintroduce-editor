import React from 'react';
import ReactDOM from 'react-dom/client';

import './index.css';
import SintroduceEditor from './components/sintroduceEditor/sintroduceEditor';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SintroduceEditor />
  </React.StrictMode>
);
